services:
  backend:
    build: ./Backend
    ports:
      - "8000:8000"
    volumes:
      - ./Backend/db.sqlite3:/app/db.sqlite3

  frontend:
    build: ./Frontend
    ports:
      - "5173:5173"
    depends_on:
      - backend
